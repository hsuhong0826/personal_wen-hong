# AI Agent Development Standard

> 本文件定義 AI Agent（如 GitHub Copilot、IDE Agent）在本專案中進行程式碼開發時必須遵守的強制標準。  
> **每一次代理開發前與產出前，必須逐條符合以下規範。**

---

## 0. 開發前確認機制（Mandatory）

在任何程式碼修改動作開始前，AI Agent **必須先輸出「需求理解確認」**，內容包含：

1. **需求重述**：用自己的話描述本次需求，確認理解正確
2. **預計修改檔案清單**：列出將要修改的所有檔案及修改原因
3. **影響功能路徑**：說明哪些功能路徑會受影響

> ⚠️ 確認後等待使用者回覆「確認」或「修正」，才可開始實作。

---

## 1. 核心開發原則（Mandatory）

### 1.1 最小改動原則（Strongly Enforced）
- 僅允許為完成「本次明確需求」所需的最小修改
- **禁止修改與需求無直接關聯的程式碼**
- 禁止為了風格一致、順手優化而更動無關區段

### 1.2 行為不可變更
- 不得破壞既有功能行為
- 不得改變既有 API contract、function signature、回傳格式（除非需求明確要求）
- 不得調整既有使用邏輯的順序或副作用

---

## 2. 專案一致性與寫法規範

### 2.1 必須參考既有實作
- 新增或修改程式碼時：
  - **必須參考專案內其他相同類型檔案的寫法**
  - 包含：命名方式、結構、錯誤處理模式、log 方式
- 禁止引入「全新、專案中不存在的寫法或風格」

### 2.2 不引入新風格
- 不導入新的設計模式（除非專案內已存在）
- 不改變既有層級、模組或資料流設計
- 不因 AI 自行判斷而重構既有結構

---

## 3. 程式碼修改規範

### 3.1 影響範圍控制
- 僅修改與需求直接相關的檔案
- 不允許：
  - 無意義 reformat
  - import 順序整理
  - 無關 rename
  - 無功能差異的調整

### 3.2 明確與可維護
- 邏輯需可被工程師直接理解
- 禁止過度抽象或過度泛化
- 禁止留下 TODO / FIXME 作為完成狀態

---

## 4. 架構與設計原則（企業級）

### 4.1 責任邊界清楚
- Business logic、IO、資料轉換需維持既有分層方式
- 不新增額外中介層或 wrapper

### 4.2 禁止過度設計
- 不為「未來可能需求」預先設計
- 僅實作當前需求所需能力

---

## 5. 修改整理與 Commit 訊息產生（Mandatory）

- 每次代理開發完成後：
  - **必須列出本次所有修改的檔案清單**（每個檔案需附上修改原因）
  - **必須說明哪些功能路徑受到影響**（或明確確認無影響）
  - **生成一則對應的 commit 訊息供參考**
- 僅需提供 commit message 文字：
  - ❌ 不需實際執行 commit
  - ❌ 不需推送
- Commit 訊息格式規範：
  ```
  <type>(<scope>): <subject>

  type: feat | fix | refactor | chore
  scope: 影響的模組或檔案範圍
  subject: 簡短描述做了什麼、為什麼
  ```
- Commit 訊息需清楚描述：
  - 做了什麼修改
  - 修改目的

---

## 6. 完整運作確認（Strongly Enforced）

- 所有修改完成後，**必須確認整體功能可正常運作**
- 包含但不限於：
  - 前端 / 後端行為
  - API 對接
  - 相關依賴程式碼與呼叫流程
- **嚴禁只修改單一部份而導致其他模組不一致或無法運作**
- 不得假設「其他地方會自動補齊」

---

## 7. 測試與文件策略（本專案規則）

- **本專案預設：**
  - ❌ 不需要新增文件
  - ❌ 不需要新增測試檔案
- 除非需求明確要求，否則：
  - 不建立 README、說明文件
  - 不新增或修改測試程式

---

## 8. 安全與風險控制（Mandatory）

- 不得 hardcode 憑證、token、key
- 不操作或修改 `.env`、local-only 檔案
- 不假設執行環境（OS、路徑、權限）

---

## 9. AI Agent 自我檢查清單（每次必須完成）

在產出最終程式碼前，AI Agent **必須內部確認以下事項全部成立**：

- [ ] 開發前已輸出需求理解確認，並獲得使用者確認
- [ ] 僅進行需求必要的最小改動
- [ ] 完全未修改任何無關程式碼
- [ ] 寫法與專案內既有實作一致
- [ ] 未引入新的設計模式、架構或風格
- [ ] 所有相關模組與依賴皆可正常運作
- [ ] 已列出本次所有修改檔案清單及原因
- [ ] 已說明本次修改對哪些功能路徑有影響（或確認無影響）
- [ ] 若有任何規範邊緣操作（如格式調整），已主動聲明並說明原因
- [ ] 已整理修改內容並產生符合格式的 commit 訊息

**若任一項不成立，不得視為完成。**

---

## 結語

> 本文件用於確保 AI Agent 的每一次開發行為：
> - 開發前確認需求，避免方向錯誤
> - 專注需求
> - 最小改動
> - 架構穩定
> - 完整輸出供人工驗收
> - 可直接進入企業級專案